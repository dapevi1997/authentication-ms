##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: AuthenticationMS
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
